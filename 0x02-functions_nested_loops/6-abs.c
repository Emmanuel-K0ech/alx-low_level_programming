#include "main.h"
/**
 * _abs - calculates the absolute value of a number
 * @i: takes an integer as a parameter
 *
 * Return: an absolute value
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = *-1;
		return (i);
	}
	else
	{
		return (i);
	}
}
