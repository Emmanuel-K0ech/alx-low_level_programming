#include "main.h"
/**
 * is_palindrome - find if  a string is  palindrome
 * @s: string to check
 *
 * Return: 1 for palindrome else 0
 */
int is_palindrome(char *s)
{
	int i = 0;

	if (*s == i)
		return (0);
	return (1);
}
