#include "main.h"
/**
 * _strcpy - copies string from one locatin to another
 * @dest: destination (locatin to  copy to)
 * @src: location to copy from
 *
 * Return: pointer to destination
 */
char *_strcpy(char *dest, char *src)
{
	char s1[98];

	s1 = *src;
	dest = s1;

	return (dest);
}
