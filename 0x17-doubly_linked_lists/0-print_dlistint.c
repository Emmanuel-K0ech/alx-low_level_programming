#include "lists.h"
/*
 * size_t print_dlistint - print the list
 * @h: head, pointer to first node
 *
 * Return: number of nodes
 */
size_t print_dlistint(const dlistint_t *h)
{
	size_t count;
	
	while (next != NULL)
	{
		printf("%d\n", *h);
		h->next
}
